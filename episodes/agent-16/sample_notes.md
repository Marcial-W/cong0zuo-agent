# Agent 入门笔记

Agent 不是只会聊天的机器人。它会围绕一个目标拆步骤，并决定下一步要不要调用工具。

在一个最小知识库 Agent 里，工具可以先从读取本地 Markdown 文件开始。这样输入不再只靠复制粘贴。

当资料变长以后，Agent 不应该把整篇文章都塞给模型。更稳妥的做法是先根据问题检索相关片段，再把片段放进 prompt。

真实 API 接入时，密钥要放在环境变量里。调用失败时，程序应该能 fallback 到 fake_llm，让 demo 仍然可以跑完。
